Implement 
```
class UnionFind {
  // Connect a and b
  union (a, b)
  
  // Check if a and b are connected
  connected (a, b)
  
  // Return the parent of a
  find (a)
  
  // Return the count of connected union sets
  get count ()
}
```
