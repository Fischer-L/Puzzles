Implement `MinHeap` and `MaxHeap`
```js
class MinHeap {
  insert (num) {
    // Insert one integer into the heap
  }
  
  poll () {
    // Return then delete the min value the heap
  }
  
  peek () {
    // Return the min value in the heap
  }
  
  get size () {
    // Return the heap current size
  }
}

class MaxHeap {
  insert (nums) {
    // Insert an integer into the heap
  }
  
  poll () {
    // Return then delete the current max value in the heap
  }
  
  peek () {
    // Return the current max value in the heap
  }
  
  get size () {
    // Return the current heap size
  }
}
```
